[package]
name = "proto-tower-grpc"
version = "0.1.1"
edition = "2021"
authors = [
    "RapidRecast <hugh@rapidrecast.io>",
]
description = "Tower implementation of the gRPC protocol"
license = "GPL-3.0-only"

[features]
data = []
client = ["data"]
server = ["data"]

[dependencies]
